{% extends 'base.html.twig' %}

{% block title %}Edit Events{% endblock %}

{% block body %}
    <h1 class="text-center">Edit Events</h1>
   
    {{ include('events/_form.html.twig', {'button_label': 'Update'}) }}

   

   <div class="text-center own">
    <br>
    <a class="btn btn-success fs-3" href="{{ path('app_events_index') }}">Back to events</a>

    {{ include('events/_delete_form.html.twig') }}
    </div>
{% endblock %}
